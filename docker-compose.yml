services:
  comicsbox:
    container_name: comicsbox
    build:
      context: .
      dockerfile: Dockerfile
    image: comicsbox:latest
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Production
    restart: unless-stopped